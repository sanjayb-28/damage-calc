{
  "name": "pkmn-dmg-calc-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:desktop": "node scripts/prepare-desktop.mjs",
    "preview": "npm run prepare:desktop && http-server dist -p 4173 -c-1",
    "test": "node --test tests/*.test.mjs",
    "release:check": "node scripts/validate-release.mjs",
    "version:next": "node scripts/next-version.mjs",
    "version:set": "node scripts/set-version.mjs",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "http-server": "14.1.1"
  }
}
