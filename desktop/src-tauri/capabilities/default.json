{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main calculator window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
